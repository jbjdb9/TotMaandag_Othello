import java.io.IOException;

public class OthelloAI {
    public static int[] move() throws IOException {
        int[] move = {0,0};
        if (Play.remote){
            //Remote.aiMoved(move);
        }
        return move;
    }
}
